# 💊 HomeMeds AI: 家庭智慧药箱助手

> **不再让药箱成为“黑洞”。**
> 一个基于 Python 和 LLM 的家庭药品全生命周期管理系统。结合数字化库存管理与 AI 医疗决策辅助，解决“买了忘、过期不知、生病找不到药”的生活痛点。

---

## 📖 项目简介 (Introduction)

**HomeMeds AI** 是一个本地化优先的智能应用。它不仅仅是一个 Excel 替代品，更是一个具备初步推理能力的家庭健康 Agent。

核心理念是 **"AI-First"**：当用户感到不适时，不需要在大脑中搜索药名，也不需要逐个翻看药盒说明书，只需告诉系统症状，AI 将结合**现有库存**（Inventory）和**药物功效**（Knowledge Base），给出最佳用药建议及存放位置。

### 核心痛点解决

* ❌ **拒绝浪费：** 告别“家里有药却去医院开新药”的循环。
* ⚠️ **过期预警：** 交通灯式视觉管理，提前识别临期药物。
* 🧠 **智能决策：** 基于 RAG (检索增强生成) 思路，回答“半夜牙痛我该吃库存里的哪盒药”。

---

## 🛠 技术架构 (Tech Stack)

本项目采用轻量级 **MVC 架构**，适合家庭服务器（如树莓派）或本地 PC 运行。

* **开发语言:** Python 3.10+
* **前端交互:** [Streamlit](https://streamlit.io/) (极速构建数据流 Web App，支持移动端访问)
* **数据存储:** SQLite 3 (原生、轻量、单文件易备份)
* **AI 核心:** LLM API (支持 OpenAI / DeepSeek / Moonshot 等兼容接口)
* **数据处理:** Pandas

---

## ⚡️ 功能特性 (Features)

### ✅ v0.1 基础功能 (MVP)

* **可视化库存看板:** 表格化展示所有药品，支持按过期时间排序。
* **状态监测:**
* 🔴 **已过期:** 红色高亮，建议丢弃。
* 🟡 **临期 (90天内):** 黄色预警，优先使用。
* 🟢 **状态良好:** 绿色标识。


* **物理寻址:** 明确记录每盒药的存放位置（如：客厅电视柜、主卧抽屉）。

### 🚀 v0.2 AI 增强 (Current Focus)

* **AI 药剂师对话:**
* 用户输入自然语言症状（如：“孩子发烧39度，有些咳嗽”）。
* 系统检索**未过期**库存。
* LLM 分析药理作用，推荐合适药物并告知位置。


* **智能录入:** 预留 `effect_text` 字段，用于存储 OCR 识别的说明书摘要。

---

## 📂 项目结构 (Structure)

```text
HomeMeds_AI/
├── data/
│   └── medicines.db           # SQLite 数据库文件 (自动生成)
├── src/
│   ├── __init__.py
│   ├── database.py            # 数据库初始化与连接管理
│   ├── models.py              # 数据模型 (DataClass) 定义
│   ├── services.py            # 核心业务逻辑 (CRUD, 过期计算)
│   └── ai_engine.py           # LLM 接口与 Prompt 工程模块
├── app.py                     # Streamlit 启动入口 (UI 布局)
├── .env                       # API Key 配置文件 (不上传 Git)
├── requirements.txt           # 项目依赖
└── README.md                  # 项目说明书

```

---

## 💾 数据库设计 (Database Schema)

表名: `inventory`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| **id** | INT | 主键，系统自增唯一 ID |
| **name** | STR | 药品通用名 (如：布洛芬缓释胶囊) |
| **brand** | STR | 品牌 (如：芬必得) |
| **barcode** | STR | 条形码 (用于扫码枪扩展) |
| **effect_text** | STR | **[核心]** 功能主治/适应症摘要，AI 决策依据 |
| **expiry_date** | DATE | 过期日期 |
| **location** | STR | 存放位置 |
| **quantity** | STR | 剩余量描述 (如：满, >50%, 少量) |
| **is_opened** | BOOL | 是否开封 |

---

## 🚀 快速开始 (Quick Start)

### 1. 克隆项目

```bash
git clone https://github.com/yourusername/HomeMeds-AI.git
cd HomeMeds-AI

```

### 2. 环境配置

建议使用 Conda 或 venv 创建虚拟环境：

```bash
conda create -n meds python=3.10
conda activate meds
pip install -r requirements.txt

```

### 3. 配置 API Key

在根目录创建 `.env` 文件，填入你的 LLM API Key：

```ini
# .env 文件
LLM_API_KEY="sk-xxxxxxxxxxxxxxxx"
LLM_BASE_URL="https://api.deepseek.com/v1"  # 示例：使用 DeepSeek

```

### 4. 运行应用

```bash
streamlit run app.py

```

运行后，浏览器将自动打开 `http://localhost:8501`，手机连接同一 WiFi 即可访问。

---

## 📅 开发路线图 (Roadmap)

* [ ] **Phase 1:** 完成数据库建模与 Streamlit 录入/展示界面。
* [ ] **Phase 2:** 接入 LLM API，实现“基于库存的症状问答”。
* [ ] **Phase 3:** 引入 OCR 功能，上传药盒照片自动填表 (OpenCV/PaddleOCR)。
* [ ] **Phase 4:** 语音交互支持 (Whisper)。

---

## 🤝 贡献与协议

本项目仅供学习与家庭个人使用。
**免责声明：** AI 推荐仅供参考，不构成专业医疗建议。用药前请务必阅读纸质说明书或咨询医生。

License: MIT